# 多语言翻译工具 — Python 依赖
# 安装: pip install -r requirements.txt

# 核心依赖（必需）
requests>=2.28
pystray>=0.19
Pillow>=10.0

# PDF 处理（PDF 翻译功能需要）
PyMuPDF>=1.24
pdfplumber>=0.11
pdfminer.six>=20231228
pypdfium2>=5.0

# OCR 识别（图片型 PDF 需要，体积较大约 2GB）
# 如果不需 OCR 功能可以不装，图片型 PDF 会跳过
easyocr>=1.7
torch>=2.0
torchvision>=0.15
numpy>=1.24
opencv-python-headless>=4.8
scipy>=1.10

# 运行方式:
#   python translate_gui.py
# 打包方式（需要 PyInstaller）:
#   pip install pyinstaller
#   python update_exe.py
