# 多语言翻译工具 — 更新日志

---

## v12.5 — 2026-08-01

### UI 重构
- **日志分离**：XML 翻译日志和 PDF 翻译日志拆分为各自的独立区域，嵌入对应翻译页面内，随标签页切换自动切换
- **按钮布局**：清除日志和开始翻译按钮移到日志区域上方，操作更直观
- **弹性布局**：窗口缩放仅影响日志区域大小，其他控件保持固定高度均匀分布
- **状态上移**：网络状态、翻译源列表、当前 API、翻译进度等信息从底部状态栏移到标题栏设置齿轮旁边，界面更紧凑
- **删除底部状态栏**：冗余页脚已移除，所有信息整合到头部

### 翻译狗修复
- **测试连接修复**：文档翻译测试连接改用 `detectDocPage` API 发送页数参数验证连通性
- **删除余额查询**：移除账户余额查询功能（UI 及其后端方法）

### 部署
- `update_exe.py` 目标路径更新为 `D:\麦伟鹏\Python\多语言翻译`
- 增量打包 0.03 秒完成更新

---

## v12.3 — 2026-07-31

### 精简 + 优化
- **删除聊天窗口**：大模型设置页聊天对话窗口已移除（之前改了没生效，现彻底删除）
- **API Key 明文**：API Key 输入框不再隐藏，直接显示
- **启动优化**：PyMuPDF (fitz) 改为懒加载（仅在 PDF 翻译时才导入），缩减启动时间
- **打包模式**：改用 `--onedir`，EXE 45MB（依赖在 `_internal/` 文件夹），启动不再需要提取文件

### 部署
- `C:\Users\sf-bc4\Desktop\多语言翻译\多语言翻译工具.exe` — 45MB onedir 模式
- 依赖沉淀在 `_internal/` 文件夹中，后续改代码无需重打包

---

## v12.2-fix — 2026-07-31

### 修复部署路径问题
- **问题**：桌面上同时存在旧 onefile EXE（283MB）和新 onedir 文件夹，用户启动的是旧版
- **解决**：删除旧 onefile，将 onedir 内容（EXE + _internal）移到根目录
- `update_exe.py` / `启动翻译工具.bat` 路径同步修正为根目录
- 增量更新已重新部署到正确位置

---

## v12.2 — 2026-07-31

### 大模型聊天窗口改微信风格
- **类微信对话界面**：头像 + 气泡 + 时间戳，完全模仿微信布局
- **用户消息**：绿色气泡右侧，头像在最右边（皮卡丘 🟡）
- **模型消息**：白色气泡左侧，头像在最左边（模型首字彩色圆形头像）
  - `豆` → 橙色（豆包系列）
  - `深` → 青色（DeepSeek 系列）
  - `智` → 灰色（GLM 系列）
  - `小` → 蓝色（MiniMax 系列）
  - `?` → 紫色（自定义/未知）
- **时间戳**：每条消息上方居中显示发送时间
- **背景色**：统一微信浅灰 `#EDEDED`
- **头像实现**：Canvas 绘制 36×36 圆形 + 粗体文字

---

## v12.1 — 2026-07-31

### 大模型下拉栏更新
- 根据 2026 最新火山方舟模型清单，精选 10 款翻译适配模型
- 新增模型能力提示（选中/输入时自动显示描述）
- 调整后的模型列表：

| 系列 | 模型 | 定位 |
|------|------|------|
| Doubao 2.1 | doubao-seed-2-1-pro-260628 | 旗舰翻译（推荐） |
| Doubao 2.1 | doubao-seed-2-1-turbo | 均衡性价比 |
| Doubao 2.0 | doubao-seed-2-0-pro-260215 | 成熟旗舰 |
| Doubao 2.0 | doubao-seed-2-0-lite-260428 | 低成本轻量 |
| 旧代 | doubao-seed-1-8-251228 | 稳定兼容 |
| DeepSeek | deepseek-v4-pro-260425 | 100万上下文旗舰 |
| DeepSeek | deepseek-v4-flash-260425 | 低成本高并发 |
| 智谱 | glm-5-2-260617 | 100万上下文 |
| MiniMax | minimax-m3 | 长文本均衡 |
| 自定义 | ep- | Endpoint ID |

---

## v12.0 — 2026-07-31

### 新增
- **火山引擎大模型翻译**：集成豆包/方舟大模型作为第 6 个翻译源，使用 OpenAI 兼容 Chat Completions API
- **大模型设置页**（设置弹窗第三个 Tab）：独立于 XML/PDF 设置的专属页面
  - API Key 输入（密码掩码）
  - 模型下拉选择（预设 deepseek-v4-pro-260425、doubao 系列等多个常用模型，也支持手动输入）
  - 测试连接按钮
  - **模型对话窗口**：类微信聊天界面，可直接与配置的大模型对话测试
    - 用户消息绿色气泡（右侧）、模型回复白色气泡（左侧）
    - 支持多轮对话上下文、Ctrl+Enter 快捷发送、滚轮滚动消息区

### 改进
- 模型选择从文本输入框改为下拉栏（Combobox），预设常用模型

### 架构
- **增量打包架构**：改用 `--onedir` 模式
  - 首次完整打包后，依赖（PyTorch/EasyOCR/PyMuPDF）沉淀在 `_internal/` 文件夹
  - 之后只改代码时运行 `update_exe.py`，2 秒内完成更新
  - 只有安装新依赖时才需重新完整打包

---

## v11.8 — 2026-07-30

### 修复
- **PDF 翻译过滤优化**：5 层文本提取全部改为仅保留含中文字符的文本块，不再翻译纯英文/LOGO/数字
- **白底遮罩透明度提升**：`fill_opacity` 从 0.85 提升至 0.97，几乎覆盖原始中文
- **窗口尺寸调整**：默认 720x830，最小 680x790，解决状态栏被遮挡问题

---

## v11.7 — 2026-07-30

### 修复
- **白底矩形覆盖问题**：改为行级合并遮罩条，减少对电路图/图框线条的覆盖

---

## v11.6 — 2026-07-30

### 修复
- **PDF 翻译乱码**：修复 4 个关键 bug
  - 溢出检测逻辑反转（`rc > 0` 误判为溢出）
  - 静默异常吞没（`except: pass` 改为日志 + 回退方案）
  - 字体选择错误（全用 `china-s`，改为英/中分离）
  - 图像类 PDF 缺 `clean_contents()` 导致渲染层丢失

---

## v11.5 及更早

- 多翻译 API 源（MyMemory、LibreTranslate、Google、百度、翻译狗）
- XML / PDF 双语翻译
- 系统托盘、设置持久化（`settings.json`）
- 翻译源优先级拖拽排序、启用/禁用切换
- 百度翻译密钥、翻译狗 APP ID/密钥配置
- 强制重新翻译开关
- EasyOCR 图像文字识别（PDF 场景）
